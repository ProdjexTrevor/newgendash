{
  "name": "newgeneration",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n api,ui -c green,blue \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
