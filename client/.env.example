# Dev: Vite proxies /api to localhost (see vite.config.ts)
# Production on Vercel: leave unset — UI and API share the same domain
# Production elsewhere: set to your API origin, no trailing slash
# VITE_API_URL=https://your-api.example.com
